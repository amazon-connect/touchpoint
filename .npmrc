# @nlxai/core (and other @nlxai runtime deps) are published to public npm.
# Override any global scope mapping (e.g. GitHub Packages) so installs resolve here.
@nlxai:registry=https://registry.npmjs.org/
